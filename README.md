# Nimiq-Wallet-Manager

## Installation
Setup a fresh MongoDB database.

`$ sudo apt-get install -y build-essential mongodb`

`$ npm install`

`$ cp .env.sample .env`

`$ nano .env`

`$ npm run watch`
